apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-aks-hello-world.fullname" . }}
  labels:
    {{- include "api-aks-hello-world.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-aks-hello-world.selectorLabels" . | nindent 4 }}
